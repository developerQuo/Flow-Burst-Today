import { describe, expect, it, test } from '@jest/globals';
import { render, screen } from '@testing-library/react';

describe('navigation', () => {
	beforeEach(() => {});

	it('presents the nav infomation that is clicked', () => {});

	test("user's moved to the link's page by clicking the link", () => {});
});
